{
  "scripts": {
    "test": "vitest",
    "start": "tsx src/index.ts",
    "compile:rules": "tsx -e \"import { compileRulesBible } from './src/ai/rulesPipeline'; compileRulesBible().then(path => console.log('Compiled Rules:', path)).catch(error => { console.error(error); process.exit(1); })\""
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.23.5",
    "typescript": "^5.7.3",
    "vitest": "^1.6.0"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "openai": "^7.4.0"
  }
}
